"""Local package for inflow forecasting utilities and scripts.

Ensures `modelzoo` is importable when running as a module
(`python -m modelzoo.run`) or when `PYTHONPATH` points to the repo root.
"""

